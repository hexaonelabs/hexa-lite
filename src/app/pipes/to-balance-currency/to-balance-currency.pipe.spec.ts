import { ToBalanceCurrencyPipe } from './to-balance-currency.pipe';

describe('ToBalanceCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ToBalanceCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
