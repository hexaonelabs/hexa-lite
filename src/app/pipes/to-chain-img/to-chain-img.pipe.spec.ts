import { ToChainImgPipe } from './to-chain-img.pipe';

describe('ToChainImgPipe', () => {
  it('create an instance', () => {
    const pipe = new ToChainImgPipe();
    expect(pipe).toBeTruthy();
  });
});
